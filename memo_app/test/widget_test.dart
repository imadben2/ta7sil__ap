// This is a basic Flutter widget test.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts test', (WidgetTester tester) async {
    // TODO: Add tests when app is ready
    expect(true, true);
  });
}
